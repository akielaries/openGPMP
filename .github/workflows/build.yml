name: All RM-pkg Modules

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: initalize github repo
        uses: actions/checkout@v3

      - name: configure
        run: ./configure.sh
    
      - name: build all
        run: make arith make la-ops


